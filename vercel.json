{
  "version": 2,
  "builds": [
    {
      "src": "server/src/**/*.ts",
      "use": "@vercel/node"
    },
    {
      "src": "client/index.html",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "client/dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server/src/$1"
    },
    {
      "src": "/(.*)",
      "dest": "client/$1"
    }
  ]
}
